namespace Catslandx.Script.CharacterController {

  /** Enum identifies the function of the sensor. */
  public enum SensorEnum {
    // Detect whether it is on the ground.
    ON_GROUND_SENSOR,

    // Detect the celling.
    CEILING_SENSOR,

    // Detect the relay point.
    RELAY_SENSOR,

	// Detect the left side.
	REAR_SENSOR,

	// Detect the right side.
	FRONT_SENSOR,
  }
}
